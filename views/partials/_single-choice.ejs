<section>
    <h3>二、选择题</h3>

    <% topics.forEach(function(topic,i){ %>
    <ul>
        <label><%= i+1 %>.<%= topic.question %></label>

        <% for(var choice in topic.options) { %>
        <li>
            <input name="single_choice<%= i %>" type="radio" value="<%= choice%>"/>
            <label>（<%= choice %>）<%= topic.options[choice] %></label>

        </li>
        <% } %>

    </ul>
    <% }); %>

</section>
